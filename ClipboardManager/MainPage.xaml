<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ClipboardManager.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClipboardManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource SolidBackgroundFillColorSecondaryBrush}"
    Loading="Page_Loading">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="33"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border 
            CornerRadius="2" 
            Height="2"
            Background="White"
            MaxWidth="100"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            Opacity="0.7"/>

        <Pivot SelectedIndex="2" Grid.Row="1">
            <PivotItem Margin="0" Padding="5,3,5,0">
                <PivotItem.Header>
                    <FontIcon Glyph="&#xe713;" FontSize="16"/>
                </PivotItem.Header>

                <Frame>
                    <local:SettingsPage/>
                </Frame>
            </PivotItem>
            
            <PivotItem Margin="0">
                <PivotItem.Header>
                    <FontIcon Glyph="&#xf0e3;" FontSize="16"/>
                </PivotItem.Header>

                <ListView x:Name="ClipboardHistoryListView" SelectionMode="None" Grid.Row="1">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <Grid Background="{ThemeResource SolidBackgroundFillColorTertiaryBrush}" CornerRadius="{StaticResource OverlayCornerRadius}" BorderBrush="{ThemeResource ExpanderHeaderBorderBrush}" BorderThickness="1" Padding="7,5" Margin="0,3,0,3">
                                <Grid.Transitions>
                                    <TransitionCollection>
                                        <RepositionThemeTransition/>
                                    </TransitionCollection>
                                </Grid.Transitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="1*"/>
                                    <RowDefinition Height="1*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="{x:Bind}" IsTextSelectionEnabled="True" Grid.Column="0" MaxHeight="60" TextWrapping="Wrap"/>

                                <StackPanel Grid.Column="1" Grid.RowSpan="2" Orientation="Vertical" Spacing="5">
                                    <Button Style="{StaticResource IconButtonStyle}">
                                        <FontIcon Glyph="&#xe8c8;" FontSize="14"/>
                                    </Button>
                                    <Button Style="{StaticResource IconButtonStyle}">
                                        <FontIcon Glyph="&#xe718;" FontSize="14"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </PivotItem>

            <PivotItem Padding="0" Margin="0">
                <PivotItem.Header>
                    <FontIcon Glyph="&#xf6fa;" FontSize="16"/>
                </PivotItem.Header>

                <Grid>
                    <Grid x:Name="CreateActionsGrid" Padding="5,0,5,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <InfoBar 
                            Severity="Warning" 
                            Grid.Row="0" 
                            Title="No clipboard actions have been created, please create at least one action to use clipboard actions."
                            IsOpen="True"
                            IsClosable="False"/>
                        <TextBlock Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.6" Text="Create action" Margin="0,12,0,0" Grid.Row="1"/>
                        <StackPanel Grid.Row="2" Spacing="15" Background="{ThemeResource SubtleFillColorTertiaryBrush}" Padding="16" CornerRadius="{StaticResource ControlCornerRadius}">
                            <Grid RowSpacing="3">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Style="{ThemeResource CaptionTextBlockStyle}" Text="Label"/>
                                <TextBox Grid.Row="1"/>
                            </Grid>
                            <Grid RowSpacing="3">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Style="{ThemeResource CaptionTextBlockStyle}" Text="Format"/>
                                <TextBox Grid.Row="1"/>
                            </Grid>
                            <Grid RowSpacing="3">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Style="{ThemeResource CaptionTextBlockStyle}" Text="Regex"/>
                                <TextBox Grid.Row="1"/>
                            </Grid>
                            <Button Style="{StaticResource AccentButtonStyle}" Content="Add" HorizontalAlignment="Right" Padding="16,5,16,7"/>
                        </StackPanel>
                    </Grid>

                    <ListView x:Name="ListView" SelectionMode="None" ItemsSource="{x:Bind Actions}" Visibility="Collapsed"/>
                </Grid>
            </PivotItem>
        </Pivot>
    </Grid>
    
</Page>
