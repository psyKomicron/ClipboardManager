<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ClipboardManager.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClipboardManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource SolidBackgroundFillColorSecondaryBrush}"
    Loading="Page_Loading">

    <ListView x:Name="ListView">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="x:String">
                <Grid Background="{ThemeResource SolidBackgroundFillColorTertiaryBrush}" CornerRadius="{StaticResource ControlCornerRadius}" BorderBrush="{ThemeResource AccentControlElevationBorderBrush}" BorderThickness="1" Padding="7,5" Margin="0,3,0,3">
                    <Grid.Transitions>
                        <TransitionCollection>
                            <RepositionThemeTransition/>
                        </TransitionCollection>
                    </Grid.Transitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock IsTextSelectionEnabled="True" Grid.Column="0" Text="{x:Bind}" MaxHeight="60" TextWrapping="Wrap"/>
                    <TextBlock IsTextSelectionEnabled="True" Grid.Column="0" Grid.Row="1">[A-z0-9]{5}</TextBlock>

                    <StackPanel Grid.Column="1" Grid.RowSpan="2" VerticalAlignment="Center">
                        <Button Style="{StaticResource IconButtonStyle}">
                            <FontIcon Glyph="&#xe8ad;" FontSize="14"/>
                        </Button>
                        <Button Style="{StaticResource IconButtonStyle}">
                            <FontIcon Glyph="&#xe70f;" FontSize="14"/>
                        </Button>
                    </StackPanel>

                    <StackPanel Grid.ColumnSpan="2" Grid.Row="2" Orientation="Horizontal" Spacing="5" VerticalAlignment="Top" HorizontalAlignment="Stretch">
                        <Button HorizontalAlignment="Stretch" MaxWidth="150">Edit regex</Button>
                        <Button HorizontalAlignment="Stretch" MaxWidth="150">Edit url</Button>
                    </StackPanel>
                </Grid>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
    
</Page>
