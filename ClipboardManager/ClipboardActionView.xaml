<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ClipboardManager.ClipboardActionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClipboardManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loading="UserControl_Loading">

    <Grid 
        Background="{ThemeResource SolidBackgroundFillColorTertiaryBrush}" 
        CornerRadius="{StaticResource OverlayCornerRadius}" 
        BorderBrush="{ThemeResource ExpanderHeaderBorderBrush}" 
        BorderThickness="1" 
        Padding="7,5" 
        Margin="0,3,0,3">
        <Grid.Transitions>
            <TransitionCollection>
                <RepositionThemeTransition/>
            </TransitionCollection>
        </Grid.Transitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock 
            Text="{x:Bind Text}"
            FontSize="17"
            IsTextSelectionEnabled="True" 
            Grid.Column="0"
            MaxHeight="60" 
            TextWrapping="Wrap" 
            Grid.Row="0" 
            Margin="3,5,0,0"/>

        <Button Style="{StaticResource IconButtonStyle}" Grid.Column="1" Grid.Row="0">
            <FontIcon Glyph="&#xe70f;" FontSize="14"/>
        </Button>

        <GridView 
            Grid.ColumnSpan="2"
            Grid.Row="1"
            VerticalAlignment="Top" 
            HorizontalAlignment="Stretch"
            SelectionMode="None"
            HorizontalContentAlignment="Stretch">
            <GridView.ItemContainerTransitions>
                <TransitionCollection>
                    <RepositionThemeTransition IsStaggeringEnabled="False"/>
                </TransitionCollection>
            </GridView.ItemContainerTransitions>
            <Button HorizontalAlignment="Stretch" MaxWidth="150" Padding="15,5" Content="Edit regex"/>
            <Button HorizontalAlignment="Stretch" MaxWidth="150" Padding="15,5" Content="Edit url"/>
            <Button HorizontalAlignment="Stretch" MaxWidth="150" Padding="15,5" Content="Edit url"/>
            <Button HorizontalAlignment="Stretch" MaxWidth="150" Padding="15,5" Content="Edit url"/>
        </GridView>
    </Grid>
</UserControl>
