<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ClipboardManager.ClipboardActionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClipboardManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loading="UserControl_Loading">

    <Grid 
        Background="{ThemeResource SolidBackgroundFillColorTertiaryBrush}" 
        CornerRadius="{StaticResource OverlayCornerRadius}" 
        BorderBrush="{ThemeResource ExpanderHeaderBorderBrush}" 
        BorderThickness="1" 
        Padding="5,5,5,0" 
        RowSpacing="4"
        Margin="0,3,0,3">
        <Grid.Transitions>
            <TransitionCollection>
                <RepositionThemeTransition/>
            </TransitionCollection>
        </Grid.Transitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock 
            Text="{x:Bind Text}"
            IsTextSelectionEnabled="True" 
            Grid.Column="0"
            MaxHeight="60" 
            TextWrapping="Wrap" 
            Grid.Row="0" 
            Margin="3,5,0,0"/>

        <Button Style="{StaticResource IconButtonStyle}" Grid.Column="1" Grid.Row="0">
            <FontIcon Glyph="&#xe70f;" FontSize="14"/>
        </Button>

        <GridView 
            x:Name="ActionsGridView"
            Grid.ColumnSpan="2"
            Grid.Row="1"
            VerticalAlignment="Bottom" 
            HorizontalAlignment="Center"
            SelectionMode="None"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Bottom">
            <GridView.ItemContainerTransitions>
                <TransitionCollection>
                    <RepositionThemeTransition IsStaggeringEnabled="False"/>
                </TransitionCollection>
            </GridView.ItemContainerTransitions>
            <GridView.Resources>
                <Style TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                    <Setter Property="MaxWidth" Value="150" />
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="15,5" />
                </Style>
                <Style TargetType="GridViewItem" BasedOn="{StaticResource DefaultGridViewItemStyle}">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="3,0,3,0" />
                </Style>
            </GridView.Resources>

            <GridView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <Grid >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" MinWidth="60"/>
                            <ColumnDefinition Width="1"/>
                            <ColumnDefinition Width="Auto"/>
                            <!--<ColumnDefinition Width="Auto"/>-->
                        </Grid.ColumnDefinitions>
                        
                        <!--
                        <TextBlock Text="{x:Bind}" Grid.Column="0" VerticalAlignment="Center" TextWrapping="NoWrap" TextTrimming="None"/>
                        <Button Grid.Column="2" Style="{StaticResource IconButtonStyle}" Margin="0,0,3,0">
                            <FontIcon Glyph="&#xe8ad;" FontSize="14"/>
                        </Button>
                        -->
                        <Button Grid.Column="0" Content="{x:Bind}" CornerRadius="5,0,0,5" VerticalAlignment="Stretch"/>

                        <Border Grid.Column="1" Background="{ThemeResource SolidBackgroundFillColorTertiaryBrush}" Width="1" VerticalAlignment="Stretch"/>
                        <Button Grid.Column="2" CornerRadius="0,5,5,0" VerticalAlignment="Stretch">
                            <FontIcon Glyph="&#xe71b;" />
                        </Button>

                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</UserControl>
