<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Name="Control"
    x:Class="ClipboardManager.ClipboardActionEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClipboardManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="UserControl_Loaded">

    <Grid
        x:Name="RootGrid"
        Background="{ThemeResource SolidBackgroundFillColorTertiaryBrush}"
        Padding="16,10,16,16"
        CornerRadius="{StaticResource ControlCornerRadius}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Enabled"/>
                <VisualState x:Name="Disabled">
                    <VisualState.Setters>
                        <Setter Target="Control.Foreground" Value="{ThemeResource ButtonForegroundDisabled}"/>
                        <Setter Target="RemoveButton.IsEnabled" Value="False"/>
                        <Setter Target="EditButton.IsEnabled" Value="False"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <TextBlock Grid.Row="0" Style="{StaticResource BodyStrongTextBlockStyle}" FontSize="22" VerticalAlignment="Center" Text="{x:Bind ActionLabel}"/>
        
        <TextBlock x:Uid="FormatTextBlock" Grid.Row="1" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6" Margin="0,10,0,3" Text="Format"/>
        <TextBlock Grid.Row="2" VerticalAlignment="Center" Grid.ColumnSpan="2" Text="{x:Bind ActionFormat}"/>
        
        <TextBlock x:Uid="RegexTextBlock" Grid.Row="3" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6" Margin="0,7,0,3" Text="Regex"/>
        <TextBlock Grid.Row="4" VerticalAlignment="Center" Grid.ColumnSpan="2" Text="{x:Bind ActionRegex}"/>

        <StackPanel Orientation="Horizontal" Spacing="4" Grid.Column="1">
            <ToggleSwitch Grid.Column="1" Grid.Row="1" Toggled="ActionEnabledToggleSwitch_Toggled" Style="{StaticResource FlippedToggleSwitchStyle}" OnContent="{x:Null}" OffContent="{x:Null}" IsOn="True"/>

            <Border Background="{ThemeResource MenuFlyoutSeparatorThemeBrush}" Width="2" VerticalAlignment="Stretch" CornerRadius="1" Margin="6,0,3,0" MaxHeight="30"/>

            <Button x:Name="RemoveButton" Click="RemoveButton_Click" Style="{StaticResource IconButtonStyle}">
                <FontIcon FontSize="15" Glyph="&#xe74d;"/>
            </Button>

            <Button x:Name="EditButton" Click="EditButton_Click" Style="{StaticResource IconButtonStyle}">
                <FontIcon FontSize="15" Glyph="&#xe70f;"/>
            </Button>
        </StackPanel>


        <ContentDialog x:Name="EditDialog" x:Uid="EditDialog" DefaultButton="Primary">
            <Grid>
                <TextBlock Text="TODO"/>
            </Grid>
        </ContentDialog>
    </Grid>
</UserControl>
